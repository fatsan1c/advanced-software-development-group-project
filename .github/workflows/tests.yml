name: CI

on:
  push:
  pull_request:

jobs:
  format:
    runs-on: ubuntu-latest
    env:
      PYTHONUTF8: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install black
        run: python -m pip install black>=24.0.0

      - name: Format Python (black)
        run: python -m black --check .

  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    env:
      PYTHONUTF8: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install OS deps (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-tk

      - name: Install OS deps (macOS)
        if: runner.os == 'macOS'
        run: brew install tcl-tk

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r setupfiles/requirements.txt -r setupfiles/requirements-dev.txt

      - name: Run tests
        run: |
          python -m pytest --cov=paragonapartments --cov-report=term --cov-report=xml --junitxml=pytest-results.xml

      - name: Upload pytest results (junit)
        uses: actions/upload-artifact@v4
        with:
          name: pytest-results-${{ matrix.os }}
          path: pytest-results.xml

      - name: Upload coverage (xml)
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml-${{ matrix.os }}
          path: coverage.xml

